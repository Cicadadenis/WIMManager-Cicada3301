# 🐍 WIMManager-Cicada3301

> Python-powered GUI tool for managing Windows WIM images (WinRE, WinPE, install.wim) with style.

WIMManager-Cicada3301 — это утилита для удобной работы с образами **WIM** в Windows.
Приложение оборачивает команды **DISM** и **wimlib-imagex** в удобный графический интерфейс с логом, темами
и небольшими Python-приятностями.

---

## ✨ Основные возможности

- 📂 Выбор WIM-файла через стандартный диалог
- 📁 Выбор папки для монтирования
- 🔢 Ввод индекса образа (Index), по умолчанию `1`
- 🧷 Монтирование образов WIM:
  - через **DISM** (`/Mount-Wim`)
  - через **wimlib-imagex** (`mount`)
- 📤 Размонтирование:
  - **Commit** — с сохранением изменений
  - **Discard** — без сохранения изменений
- 🔍 Просмотр индексов WIM:
  - `dism /Get-WimInfo`
  - `wimlib-imagex info`
- 📜 Просмотр списка уже смонтированных WIM через `dism /Get-MountedWimInfo`
- 🎨 Светлая и тёмная темы оформления
- 🧾 Окно лога с выводом всех команд и их stdout/stderr
- 🧰 Кнопки:
  - **Установить DISM** (открытие страницы ADK)
  - **Установить wimlib** (автоскачивание и распаковка `wimlib-imagex`)

Всё это — в духе Python: минимум лишних действий, максимум пользы.

---

## 🧮 Требования

- 🪟 **Windows 10 / 11**
- 🐍 **Python 3.10+** (для работы из исходников)
- Встроенный **DISM** (обычно есть по умолчанию в Windows)
- (Опционально) **wimlib-imagex** для более быстрого и гибкого управления WIM

---

## 📦 Установка и запуск из исходников

1. Клонируем репозиторий:

   ```bash
   git clone https://github.com/USERNAME/WIMManager-Cicada3301.git
   cd WIMManager-Cicada3301
   ```

2. Убеждаемся, что Python установлен и доступен как `python` или `py`.

3. Устанавливаем зависимости (если они есть в `requirements.txt`, либо пропускаем этот шаг):

   ```bash
   pip install -r requirements.txt
   ```

4. Запускаем приложение:

   ```bash
   python mount_wim.py
   ```

> ⚠️ **Важно:** для монтирования WIM через DISM программа должна быть запущена **от имени администратора**,
> иначе DISM вернёт ошибку `740` (недостаточно прав).

---

## 🧰 Технологии

- **Python 3.x**
- `tkinter` — графический интерфейс
- `subprocess` — запуск DISM и wimlib
- `threading` — фоновые операции, чтобы GUI не зависал
- `urllib.request`, `zipfile`, `tempfile` — для загрузки и установки wimlib
- `os`, `sys`, `pathlib` — работа с путями и ресурсами
- **DISM** и **wimlib-imagex** — реальная работа с WIM

---

## 🧷 Кнопка «Установить wimlib»

При нажатии на кнопку **«Установить wimlib»** приложение:

1. Скачивает архив `wimlib-*-windows-x86_64-bin.zip` с официального сайта `wimlib`.
2. Распаковывает его в подпапку `wimlib` рядом с программой.
3. Находит `wimlib-imagex.exe` и добавляет путь к нему во внутренний `PATH` процесса.
4. Обновляет статус (`detect_tools`) и показывает, что `wimlib-imagex` найден.

После этого можно выбрать бэкенд **`wimlib`** в верхнем комбобоксе.

---

## 🧱 Сборка в .exe (PyInstaller)

Для сборки WIMManager-Cicada3301 в один `.exe` без консоли и с иконкой `logo.ico`:

1. Установи PyInstaller:

   ```bash
   pip install pyinstaller
   ```

2. Выполни сборку (из папки с `mount_wim.py` и `logo.ico`):

   ```bash
   pyinstaller --noconsole --onefile ^
     --icon=logo.ico ^
     --add-data "logo.ico;." ^
     mount_wim.py
   ```

После сборки готовый `.exe` будет лежать в папке `dist/`.

> 💡 Если используешь дополнительные файлы (например, встроенную папку `wimlib`),
> их также можно добавить через `--add-data`.

---

## 🚀 Типичный сценарий

1. Запусти программу от имени администратора.
2. Выбери WIM-файл (например, `Winre.wim`).
3. Укажи папку монтирования (должна существовать).
4. (Опционально) Нажми **«Показать индексы WIM»**, чтобы посмотреть доступные индексы.
5. Укажи нужный индекс (по умолчанию `1`).
6. Нажми **«Монтировать»**.
7. После работы нажми:
   - **«Размонтировать (Commit)»** — чтобы сохранить изменения.
   - **«Размонтировать (Discard)»** — чтобы отбросить изменения.

---

## 📝 Лог и отладка

В нижней части интерфейса есть окно лога. Там отображаются:

- запущенные команды (`dism ...`, `wimlib-imagex ...`)
- их полный вывод
- коды возврата и сообщения об ошибках

Это упрощает диагностику проблем (например, `Error: 87`, `Error: 740`, ошибки путей и индексов).

---

## 🧑‍💻 Автор

Проект создан пользователем **Cicada3301**.

Если ты Python-разработчик и хочешь доработать утилиту — можно легко расширить код:
- добавить автопоиск `Winre.wim` в текущей системе;
- добавить работу с `boot.wim` / `install.wim`;
- сделать профили путей и пресеты операций;
- добавить локализации интерфейса.

Pull request'ы и форки приветствуются 🐍

---

## ⚠️ Дисклеймер

Программа работает с системными образами Windows. Любые изменения в WIM-файлах ты делаешь **на свой страх и риск**.

Рекомендуется:

- всегда иметь резервную копию WIM;
- тестировать изменения сначала на тестовых образах;
- не трогать рабочие образы, если не уверен в результате.

Happy hacking & safe mounting 🧷🪟🐍
